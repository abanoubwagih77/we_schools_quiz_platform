rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // School IT Platform Firestore Security Rules
    // Allow read and write for authenticated school application operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
